# Kmeans
Implementation of Kmeans algorithm
Max iterations is set to a default number 200
To run C code after compilation: ./kmeans k max_iter < file_path
To run python code: python kmeans.py k max_iter file_path
